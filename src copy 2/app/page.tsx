import SpineCanvas from '@/components/Spine/SpineCanvas'

export default function Home() {
  return (
    <main className="relative w-screen h-screen overflow-hidden">
      <SpineCanvas />
      {/* Add UI overlays later here */}
    </main>
  )
}